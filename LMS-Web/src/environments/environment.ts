// Default (production) environment. The CLI replaces this file per configuration
// via fileReplacements in angular.json when building dev.
export const environment = {
  production: true,
  // Same-origin (used when SPA + API are served from the same host).
  // apiBaseUrl: '/api/v1',
  apiBaseUrl: 'https://api.tsss.com.pk/api/v1',
  siteName: 'TSSS'
};
